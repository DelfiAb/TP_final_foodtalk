import React from "react";

export const NavBar = () => {
    return(
        //aca metemos los elementos del html como siempre
        //todas las class se cambian por className xq class es una palabra reservada de js para otra cosa
        
        <nav>
            <h1>FoodTlak logo (Navbar)</h1>
        </nav>

    )

}
